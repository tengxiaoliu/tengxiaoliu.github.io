# Personal Website - tengxiaoliu.github.io

这是一个使用 [Hugo](https://gohugo.io/) 静态网站生成器和 [Congo](https://jpanther.github.io/congo/) 主题构建的个人网站。

## 📁 分支说明

- **`master` 分支**：存放源代码（配置文件、内容文件、主题等）
  - 在这里进行所有修改
  - 包含：`content/`、`config/`、`themes/`、`assets/` 等
  
- **`gh-pages` 分支**：存放生成的静态网站文件（HTML、CSS、JS）
  - 由部署脚本自动生成和更新
  - **不要直接修改此分支**

## 🚀 快速开始

### 修改网站内容

在 `master` 分支上进行修改：

1. **修改首页内容**
   - 编辑 `content/_index.md`

2. **修改关于页面**
   - 编辑 `content/about.md`

3. **添加/修改博客文章**
   - 在 `content/blog/` 目录下创建或编辑 `.md` 文件

4. **修改个人资料信息**
   - 编辑 `config/_default/languages.en.toml`
   - 可以修改：姓名、头像、简介、社交链接等

5. **修改网站配置**
   - `config/_default/config.toml` - 基本配置
   - `config/_default/params.toml` - 主题参数
   - `config/_default/menus.en.toml` - 导航菜单

### 本地预览（可选）

在部署前，可以先本地预览：

```bash
# 确保在 master 分支
git checkout master

# 启动本地服务器
hugo server

# 在浏览器中访问 http://localhost:1313
```

## 📤 部署流程

### 方法 1：使用部署脚本（推荐）

1. **提交更改**
   ```bash
   git add .
   git commit -m "你的提交信息"
   ```

2. **推送到 master 分支**
   ```bash
   git push origin master
   ```

3. **运行部署脚本**
   ```bash
   ./deploy.sh "你的提交信息"
   ```
   
   脚本会自动：
   - 生成静态文件
   - 切换到 gh-pages 分支
   - 更新静态文件
   - 提交并推送到远程

4. **完成！** 网站会在几分钟内更新

### 方法 2：手动部署

如果部署脚本有问题，可以手动操作：

```bash
# 1. 确保在 master 分支
git checkout master

# 2. 生成静态文件
hugo --minify

# 3. 切换到 gh-pages 分支
git checkout gh-pages

# 4. 删除旧文件（保留 .git）
find . -mindepth 1 -maxdepth 1 ! -name '.git' -exec rm -rf {} +

# 5. 复制新文件
cp -r ../public/* .

# 6. 提交并推送
git add .
git commit -m "Update site"
git push origin gh-pages

# 7. 切换回 master 分支
git checkout master
```

## 📝 主要文件说明

### 内容文件
- `content/_index.md` - 首页内容
- `content/about.md` - 关于页面
- `content/blog/` - 博客文章目录

### 配置文件
- `config/_default/config.toml` - 网站基本配置（URL、语言等）
- `config/_default/languages.en.toml` - 语言配置和作者信息
- `config/_default/params.toml` - 主题参数（颜色、布局等）
- `config/_default/menus.en.toml` - 导航菜单配置

### 静态资源
- `static/img/` - 图片文件
- `static/file/` - 可下载的文件（如 CV）
- `assets/css/custom.css` - 自定义样式

## 🔧 环境要求

- **Hugo**: 版本 0.152.2 或更高（Extended 版本）
- **Git**: 用于版本控制

### 安装 Hugo

**macOS (使用 Homebrew):**
```bash
brew install hugo
```

**其他系统:**
访问 [Hugo 安装文档](https://gohugo.io/installation/)

## ⚠️ 注意事项

1. **始终在 master 分支修改**
   - 不要直接在 gh-pages 分支上修改 HTML 文件
   - 所有修改应该在源代码中进行

2. **提交前检查**
   - 使用 `hugo server` 本地预览
   - 确保没有语法错误

3. **Git 认证**
   - 如果推送时提示需要认证，使用 Personal Access Token
   - 生成 Token: https://github.com/settings/tokens

4. **主题更新**
   - 如果更新主题，注意检查兼容性
   - 当前主题已修复与 Hugo 0.152.2 的兼容性问题

## 🐛 常见问题

### Hugo 命令找不到

如果提示 `hugo: command not found`，检查安装路径：

```bash
# 检查 Hugo 是否安装
which hugo

# 如果使用 Homebrew 安装，可能需要添加到 PATH
export PATH="/opt/homebrew/bin:$PATH"
```

### 部署脚本权限错误

```bash
chmod +x deploy.sh
```

### 构建错误

如果遇到构建错误：
1. 检查 Hugo 版本：`hugo version`
2. 检查配置文件语法
3. 查看错误信息中的具体文件位置

### 网站没有更新

- 等待几分钟（GitHub Pages 需要时间更新）
- 检查 gh-pages 分支是否成功推送
- 清除浏览器缓存后重试

## 📚 相关链接

- [Hugo 文档](https://gohugo.io/documentation/)
- [Congo 主题文档](https://jpanther.github.io/congo/docs/)
- [GitHub Pages 文档](https://docs.github.com/en/pages)

## 📄 许可证

个人使用项目。

---

**最后更新**: 2025-12-14

